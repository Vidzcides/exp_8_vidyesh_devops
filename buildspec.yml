version: 0.2
phases:
  install:
    commands:
      - echo "No dependencies needed."
  build:
    commands:
      - echo "Build started"
  post_build:
    commands:
      - echo "Deploying to S3..."
      - aws s3 sync . s3://exp-8-vidyesh --delete